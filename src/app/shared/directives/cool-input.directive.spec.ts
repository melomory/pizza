import { CoolInputDirective } from './cool-input.directive';

describe('CoolInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolInputDirective();
    expect(directive).toBeTruthy();
  });
});
