import { IsChickenDirective } from './is-chicken.directive';

describe('IsChickenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsChickenDirective();
    expect(directive).toBeTruthy();
  });
});
