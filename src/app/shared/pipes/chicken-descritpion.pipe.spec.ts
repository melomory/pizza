import { ChickenDescritpionPipe } from './chicken-description.pipe';

describe('ChickenDescritpionPipe', () => {
  it('create an instance', () => {
    const pipe = new ChickenDescritpionPipe();
    expect(pipe).toBeTruthy();
  });
});
